/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeFormatPìpePipe } from './DateTimeFormatPìpe.pipe';

describe('Pipe: DateTimeFormatPìpee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeFormatPìpePipe();
    expect(pipe).toBeTruthy();
  });
});
